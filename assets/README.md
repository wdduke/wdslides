# Assets

Put project-specific logo files here when needed.

The template looks for `assets/logo.pdf` and `assets/logo_long.pdf` by default. If these files are absent, `wdslides.cls` falls back to text marks configured with `\wdbrandmark{...}` and `\wdlongbrand{...}`.
